# 本地 SQLite 数据：每台机器自己生成，绝不入库。正常位置在工作区之外的
# ~/.dsh/storages/mes-plan-list/ 下——plans.db 是可重建的计划缓存，
# mail.db 存放 SMTP 设置、执行人邮箱映射和发送历史（含真实邮箱）。
# 这里是第二道保险，防止有人把它们拷进仓库。
*.db
*.db-shm
*.db-wal

# 本地 agent 指令文件：约定同步更新但不提交。
AGENTS.md
CLAUDE.md

# 规划与交接文档只在本地保留，不上传。
docs/

node_modules/

# 浏览器工具的本地日志，每次运行都会重新生成。
.playwright-cli/

.DS_Store
