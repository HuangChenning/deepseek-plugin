# 需求规划

最后更新：2026-09-02

本文记录 `mes-plan-list` 的五项后续需求、各自的实现方案、依赖关系与待决问题。
下面标注「已实测」的结论都在本机 DSH 0.1.1-rc.2 / mes 0.5.3 上验证过。

## 先决条件：发布

需求 3 说「其他人也会使用」，这项前置工作因此不只服务于需求 3——它决定了插件
能否被别人安装，也决定了 dshmarket 能否更新它。

现状：`plugins/mes-plan-list/package.json` 是 `private: true` 且没有 `version`，
安装方式是本地 `link:`。这种形态无法被任何更新机制识别。

需要执行：

- **发布渠道：GitHub Release**（已定）。dshmarket 支持作者提供的预构建 Release
  tarball，安装时优先级排在 npm 之后、完整仓库下载之前。据此需要给包加
  `version` 字段，并建立打 tag → 出 Release 的流程。
- 版本策略与 `CHANGELOG.md` 的 `[Unreleased]` 归档（backlog 里已有的未决项）。
- 收录进 [awesome-dsh-plugin](https://awesome-dsh-plugin.com) 注册表——dshmarket
  **只允许安装注册表内的源**，未收录就无法一键安装/更新。

在这项完成前，需求 3 无法落地；需求 1、2、4、5 不受影响。

## 1. mes CLI 路径配置 ✅ 已完成（2026-09-02）

用户在插件页面内配置 mes 可执行文件的绝对路径。这同时消除已记录的 PATH 风险
（`execFile('mes', …)` 依赖 PATH，GUI/launchd 启动的 DSH 可能解析不到）。

**方案**

- 存储：Host 侧 `~/.dsh/storages/mes-plan-list/config.json`。同 profile 内其他
  插件的惯例（如 dsh-cost-meter 用 `~/.dsh/storages/cost-meter/ledger.json`）。
  不放浏览器 localStorage——Host 执行 CLI 时读不到。
- 接口：`GET /api/plugins/mes-plan-list/config`、`PUT …/config`。
- 解析顺序：配置里的绝对路径 → 回落到 PATH 里的 `mes`。
- 页面：现有页面加一个可折叠的「设置」区，不使用 DSH 的设置面板。用户要求的就是
  「插件的管理页面」，而 DSH 设置卡片需要 React 与 `ctx.settingsScope`，我们的
  client 半边目前是零依赖纯 DOM，不值得为此引入打包器。

**安全（必须做，不是可选）**

这个字段让浏览器输入决定 Host 执行哪个二进制，等同于任意代码执行。保存时必须：

- 只接受绝对路径，拒绝相对路径、`~`、shell 元字符；
- 校验文件存在且可执行；
- 执行 `<path> --version` 并要求输出匹配 `mes version <semver>`，否则拒绝保存。

写入仍走 `execFile`，绝不拼 shell。

**工作量**：小。Host 侧一个配置模块 + 两个路由，页面一个设置区。

## 2. 登录状态检测 ✅ 已完成（2026-09-02）

**方案**

`mes -o json auth status` 返回结构化 JSON（已实测）：

```json
{ "schemaVersion": 1, "activeProfile": "default", "status": "ok",
  "tokenValid": true, "account": "…" }
```

- 接口：`GET /api/plugins/mes-plan-list/auth`，转译为 `{ok, loggedIn, account}`。
- 页面加载时调用一次，未登录时在查询表单上方显示提示条，并给出需要用户自己在
  终端执行的命令（`mes auth login`）。查询按钮保持可用但会失败得更明确。
- **不代替用户登录**：登录涉及凭据输入，插件不应经手密码。提示 + 让用户自己执行。

**依赖**：需求 1（路径解析共用）。可与需求 1 同批实现。

**工作量**：小。

## 3. 插件自更新

**不要自建 GitHub 更新器。** dshmarket（本 profile 已装，1.39.0）已经提供了
版本化的同源更新 API，本机实测可用：

```
GET /dsh-market/api/v1/capabilities
→ {"stability":"beta","marketVersion":"1.39.0","profile":"web",
   "features":{"check":true,"update":true,"progress":true,"rollback":true,"restart":true},
   "restart":{"supported":true,"managedBy":"market"}, "endpoints":{…}}
```

自建更新器要自己处理 pnpm、profile 写入、版本校验、失败回滚和重启——这些
dshmarket 都做了，且它对 npm 更新会校验 pnpm 实际落盘的版本，能挡住降级和镜像
滞后（`DOWNGRADE_DETECTED` / `RESOLVED_VERSION_MISMATCH`）。

**方案**

页面设置区内加更新块，流程：

1. `GET /dsh-market/api/v1/capabilities` —— 拿不到或 `features.update` 为 false
   就整块隐藏（用户可能没装 dshmarket）。
2. `GET /dsh-market/api/v1/updates?name=mes-plan-list` 显示当前版本与目标版本。
3. `POST /dsh-market/api/v1/updates` 得到 `operationId`（HTTP 202）。
4. 轮询 `GET /dsh-market/api/v1/operations?operationId=…` 直到终态，按返回的
   `refreshRequired` / `restartRequired` 提示用户；`restart.supported` 为 false
   时必须隐藏重启按钮。

**风险**

- API 自述 `stability: "beta"`，字段形状可能变。必须读 `capabilities.stability`
  和 `features` 再决定显示什么，不要硬编码假设。
- 依赖上面的「先决条件：发布」。未发布前这一块只能显示「本地开发版，不可更新」。

**工作量**：中。协议直白，但要写完整的状态机与失败呈现。

## 4. mes CLI 更新检测与更新 ✅ 已完成（2026-09-02）

**方案**

- 检测：`mes update --check`（已实测存在）。
- 更新：`mes update`。页面提供按钮。

**坑（已实测）**

- `mes update --check` **只有文本输出**，`-o json` 对它不生效——`mes -o json
  update --check` 与不加 `-o json` 输出完全相同（`✓ mes is up to date: 0.5.3`）。
  因此只能解析文本，这是脆弱点：MES 改文案就会失效。
  缓解：用 `mes --version`（输出 `mes version 0.5.3`）拿本地版本作为可靠事实，
  把 `--check` 的输出原样呈现给用户，而不是解析出结构再做判断。
- `mes update --check` 默认网络超时 30s，无网时会挂满。必须自己设更短超时。
- `mes update` 会替换正在使用的二进制。更新期间必须禁用查询按钮并串行化，避免
  一边替换一边有查询在跑。

**依赖**：需求 1。

**落地情况**：`mes-cli.js` 加了 `isUpToDate`（导出的纯函数，便于测试这条脆弱
判断）、`readCliVersion`（只读本机版本，不联网）、`readUpdateStatus`（联网检查）
和 `runMesUpdate`；路由 `GET …/cli` 与 `POST …/cli/update`。更新期间查询返回
503、重复更新返回 409，更新后重新读取版本而不是沿用旧值。

**检查更新只在用户点击时发生**（已定，2026-09-02）：打开页面只跑本地的
`mes --version`，不联网；`GET …/cli` 只返回版本，`?check=1` 才执行
`mes update --check`。因此没有缓存——用户点了就是想要当下的结果。

## 5. SQLite 本地缓存与同步 ✅ 已完成（2026-09-02）

这是五项里最大的一项，而且会改变插件的性质：从「只读直通」变成「持有本地状态
的同步器」。需要先把语义定清楚再动手。

**可行性**：Node 24 内置 `node:sqlite`（`DatabaseSync`，已实测可用），无需任何
外部依赖，不违反「两个插件之前不引入共享抽象」的约定。

**必须先澄清的问题：「增量」在这里指什么？**

`mes plan list` 的 flags 里**没有**按修改时间过滤的参数（只有 `--start-date`、
`--end-date`、`--status`、`--company-id`、`--executor-id` 等业务字段）。返回的行
里有 `createdTime` 和 `externalLastModifyTime`，但那是数据内容，不是可用的查询
游标。

因此服务端不支持真正的增量拉取。**已定：接受「按更小日期窗口重取」作为增量的
定义**——它仍是按窗口全量重取，只是窗口更小。对外措辞要照此说明，不要让使用者
以为是基于修改时间的差量同步。如果 MES 后续提供了按 mtime 过滤的接口，再实现
真正的增量。

两种同步因此都是同一个动作、不同窗口：

- 全量：覆盖一个宽日期窗口（如全年）。已实测 958 条约 5 次调用、7 秒。
- 增量：只覆盖最近 N 天的窗口。

**页面数据源（已定）**：默认读缓存，并且**页面必须显示数据更新至什么时间**。
这条是硬要求，不是可选提示——否则使用者会拿陈旧数据做判断。配一个手动刷新入口。

**MES 日期过滤语义（已实测，2026-09-02）**

`--start-date X --end-date Y` 是**完全包含**：只返回 `startDate >= X 且
endDate <= Y` 的计划，不是区间重叠。以计划 16160（07-13 → 07-25）为例：

| 窗口 | 结果 |
| --- | --- |
| 07-13 ~ 07-25 | 命中（边界相等也算） |
| 07-12 ~ 07-26 | 命中 |
| 07-13 ~ 07-24 | 未命中（end 超出） |
| 07-14 ~ 07-25 | 未命中（start 在窗口前） |

这条性质很关键，它给出**窗口单调性**：若窗口 W1 完全落在 W2 内，则
`result(W1) ⊆ result(W2)`。因此**一个大窗口的缓存可以精确服务其中任何小窗口的
查询**，本地只要复现那两个不等式即可，不需要再打 MES——过滤规则简单到可以无损
复刻，不存在猜测服务端行为的风险。

据此缓存设计为：

- 落盘只按**全状态**同步（`status` 留空），状态过滤放在本地做。否则带状态的
  返回不是窗口全集，下面的删除检测会误删。
- 查询时先找是否存在覆盖当前窗口的已同步窗口；有就本地筛选并显示该窗口的同步
  时间，没有就打 MES 取回、落盘、再本地筛选。
- 删除检测因此是精确的：同步窗口 W 后，删掉库里落在 W 内（同样的两个不等式）
  但本次未返回的行——MES 对 W 的返回就是该窗口的全集。

**存储约束（已定，2026-09-02）**

- 数据库是**每台机器自己的本地文件，不进 Git、不上传 GitHub**。落在
  `~/.dsh/storages/mes-plan-list/plans.db`，与仓库工作区分离，因此不会被误提交；
  仓库侧同时加 `*.db` 忽略规则作为第二道保险，防止有人把它拷进工作区。
- **懒创建**：插件加载时不建库。只有当用户在这台机器上第一次真正发起查询、
  需要落盘时才创建 `plans.db` 及其表结构。没查过的机器上不应存在这个文件。
- 每台机器的库是独立的，不做跨机同步；换机器等于重新从 MES 拉一次。

**同步触发与陈旧提示（已定，2026-09-02）**

- 同步先做**手动按钮**。后台定时同步作为后续增强，届时做成管理页面里的配置项，
  不在本轮范围内。
- 页面显示数据的同步时间；若该时间距当前**超过 1 天**，提示「请及时更新数据」。
- **幽灵行在同步时清理**：MES 侧删掉的计划不会主动通知，所以不做实时探测——
  每次同步窗口 W 后，删掉库里落在 W 内但本次未返回的行。
- **同步窗口自动扩展到覆盖已缓存的一切**（已定，2026-09-02）：幽灵行的根源不是
  「做了增量」，而是「同步窗口比缓存过的范围窄」。同步取「当前范围 ∪ 所有已同步
  窗口」，因此幽灵行不会在缓存任何角落堆积，用户也不必判断该做增量还是全量——
  正确性是自动的，而宽范围同步的代价只在真正缓存过宽范围时才付出。配「清空缓存」
  重置范围。实测规模：一年约 950 条 / 5 次调用 / 7 秒；十年约 8000 条 / 85 秒 /
  13MB。
- ~~首次查询尚无缓存时的行为~~（已定，2026-09-02）：**直接透传到 MES 并顺便
  落盘，不打断用户**。首次使用的观感应与现在完全一致——没有「请先同步」这类
  额外步骤，缓存是顺带产生的副作用。据此「同步」也不需要独立的前置动作：
  未命中就取并落盘，命中就读缓存，用户可手动刷新强制重取。

**落地情况**：`src/plan-store.js`（`PlanStore`，懒创建、窗口读写、幽灵行清理），
查询路径改为读缓存优先，`POST …/query` 增加 `refresh`，返回 `syncedAt` 与
`fromCache`；页面加「同步最新数据」按钮和同步时间/陈旧提示。

实现过程中查出并修掉两个正确性问题，都不是缓存本身引入的：

1. **窗口边界必须带时间**。MES 把 `--end-date 2026-07-31` 当作
   `2026-07-31 00:00:00`，因此结束于当天 18:00 的计划不在该窗口内。最初把日期
   截断到天再比较，导致一个窄窗口本地返回 11 条而 MES 只有 6 条。现在保留完整
   时间戳比较，已逐窗口、逐状态与 MES 对照一致。
2. **MES 分页会重复返回记录**。全年 958 行里有 5 个重复 id，实际只有 953 个
   计划——这是缓存之前就存在的缺陷，页面条数一直偏多。`queryPlans` 现在按 id
   去重，且终止判断用原始取回条数，否则去重会让它永远够不到 `total`。

**后续增强（不在本轮）**：后台定时同步，做成管理页面里的配置项。

## 建议实施顺序

1. ~~**需求 1 + 2**~~：已完成。`src/config.js` + `src/mes-cli.js`，两个新路由
   `/api/plugins/mes-plan-list/config`（GET/PUT）与 `…/auth`（GET），页面加了设置
   面板与登录横幅。已消除 backlog 里记的 PATH 风险。
2. ~~**需求 4**~~：已完成。见上节的落地情况。
3. **先决条件：发布**：需要你先定发布渠道与版本策略；它阻塞 3，也是「其他人使用」
   的前提。
4. **需求 3**：发布完成后接 dshmarket update API。
5. ~~**需求 5**~~：已完成。见上节的落地情况。

## 已定的三项决策（2026-09-02）

1. 发布渠道：**GitHub Release**。
2. 需求 5 的「增量」：**接受「按更小日期窗口重取」的定义**，MES 不支持按修改
   时间过滤，真正的差量同步做不了。
3. 页面默认数据源：**读缓存**，且页面必须显示数据更新至什么时间。
